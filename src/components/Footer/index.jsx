import React from 'react';
import { config } from 'config'; // eslint-disable-line

export default function Footer() {
  return (
    <footer>
      <section>
        <p />
      </section>
    </footer>
  );
}
